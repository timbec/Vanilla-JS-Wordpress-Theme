import config from '../../lib/config';
import Helpers from '../../lib/Helpers';

export default class Header {

   static render() {
      console.log('the header. Havent figured out how this works yet'); 

   }
}
